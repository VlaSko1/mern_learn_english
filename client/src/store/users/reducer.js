

const defaultState = {
  currentUser: {},
  isAuth: false
};

export const usersReducer = (state = defaultState, action) => {
  switch (action.type) {
    


    default: 
      return state;
    
  }
}